#!/bin/sh

# Когда запускается автоматически, рандомизируйте, чтобы минимизировать стадо
if [ -t 0 ]; then
    chance=10
else
    chance=$(shuf -i 0-10 -n 1)
fi

if [ $chance -gt 9 ]; then
    # Открытые вопросы для вещей, которые нужно делать на регулярной основе
    bots/npm-trigger
fi
